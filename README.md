# VIPER App (Swift, UIKit) 🐶

Учебное iOS-приложение на Swift + UIKit, реализованное с использованием архитектурного паттерна **VIPER** (View–Interactor–Presenter–Entity–Router).  

Приложение состоит из двух экранов: список изображений собак и детальный просмотр выбранного изображения. Может использоваться как пример для изучения VIPER, роутинга и базовой сетевой логики.

###  Работа первого экрана
На первом экране (MainViewController):
- При загрузке запускается сетевой запрос на https://dog.ceo/api/breed/hound/images, чтобы получить массив URL изображений собак.
- Presenter получает данные от Interactor и передаёт их во View.
- Кэширование изображений происходит через ImageLoaderService с использованием NSCache.
- Пользователь может нажать на любую ячейку, Router открывает детальный экран с выбранным изображением.


### Работа второго экрана
На детальном экране (DetailViewController) при загрузке:
- Показывается изображение, выбранное на первом экране.
- Одновременно запускается сетевой запрос на https://dog.ceo/api/breeds/image/random, чтобы получить случайную картинку собаки.
- После загрузки изображение отображается под выбранной картинкой, и пользователь может обновлять случайное изображение по кнопке.

Dog API: [https://dog.ceo/dog-api/documentation/breed](https://dog.ceo/dog-api/documentation/breed)

> Возможно, понадобится включить VPN, если данные не будут загружаться.

---

## Архитектура

В проекте используется VIPER с дополнительным контейнером зависимостей (`DependencyContainer`) для управления сервисами.

### Entity / Model

```swift
struct MainModel: Codable {
    let message: [String]
    let status: String
}

struct RandomDogModel: Codable {
    let message: String
    let status: String
}
```

## View

- MainViewController — отображает таблицу с картинками и названиями собак.
- MainTableViewCell — кастомная ячейка таблицы.
- DetailViewController — экран для отображения выбранного изображения и загрузки случайного изображения.

## Interactor

- MainInteractor — получает данные через NetworkService, предоставляет доступ к кешу изображений через ImageLoaderService.
- DetailInteractor — загружает случайные изображения и управляет загрузкой изображений через ImageLoaderService.

## Presenter

- MainPresenter — управляет данными списка, кэшированием изображений и передает их во View.
- DetailPresenter — управляет отображением выбранного изображения и загрузкой случайного изображения.

## Router
- MainRouter / DetailRouter — отвечает за навигацию между экранами (push, present, dismiss).

## Services
- NetworkService — загрузка данных из сети.
- ImageLoaderService — загрузка и кэширование изображений через NSCache.

Зачем NSCache:
- Быстрый доступ к ранее загруженным изображениям.
- Автоматическое управление памятью системой.
- Экономия трафика и ресурсов за счет повторного использования изображений.

## Dependency Injection
- DependencyContainer управляет сервисами (networkService и imageLoaderService) и передается в конфигураторы VIPER-модулей.

## Кэширование изображений
Принцип работы:
- Проверка NSCache перед загрузкой изображения.
- Если изображение есть в кеше — используется сразу.
- Если нет — загружается через сеть, сохраняется в кеш и отображается.

## Используемые технологии
- Swift 5+
- UIKit
- VIPER архитектура
- URLSession для сетевых запросов
- NSCache для кэширования изображений
- Auto Layout через NSLayoutConstraint

